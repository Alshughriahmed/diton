📌 طلب توثيق شامل (قراءة فقط) — DitonaChat

المطلوب:
أنشئ ملف توثيق واحد بصيغة Markdown (وملف JSON اختياري) يُظهر “الصورة الكاملة” لكل ما في مساحة العمل بدون أي تغييرات على الملفات. التوثيق يجب أن يُجيب بدقة عن:

ما كل مجلد؟ لماذا هو موجود؟ متى/كيف أنشئ؟ ما علاقته بوظائف المشروع؟ هل هو حي داخل السلسلة التنفيذية أم مهمل/نسخة/مخرجات/طرف ثالث؟

أين جميع الملفات الحرجة (UI/Chat/Tailwind/Legal/API/Config)؟ وما المكرّر/المفقود/المكسور؟

🎯 المخرجات المطلوبة

Markdown:
_ops/reports/FULL_TREE_AUDIT_<UTC-YYYYMMDD-HHMMSS>.md

(اختياري) JSON مُرافق:
_ops/reports/FULL_TREE_AUDIT_<UTC-YYYYMMDD-HHMMSS>.json

لا تُعدّل، لا تحذف، لا تُنقِل، لا تُثبّت حِزمًا، لا تبني المشروع. المهمة توثيق فقط.

🧭 نطاق المسح

نقطة البدء: جذر مساحة العمل كاملة (workspace).

اشمل كل شيء، خصوصًا:

_ops/exports/* (snapshots)، _ops/backups/*، ARCHIVE_OLD/، مجلدات مثل ditonachat-clean/, DitonaChat/, ditonachat_restored_*, .next, dist, public, src, node_modules, attached_assets, handovers, logs, reports، إلخ.

استخدم ذاكرة الجلسات السابقة لديك عن DitonaChat لتفكيك الالتباس عند تعدد الجذور/النسخ.

🏷️ قواعد التصنيف والوسوم

ضع وسمًا واضحًا بجانب كل عنصر (ملف/مجلد):

[LIVE] حي — ضمن الجذر التشغيلي الحالي أو snapshot المعتمَد، أو مشار إليه من:

src/app/**, src/components/**, src/hooks/**, src/utils/**

مستورد فعليًا في كود آخر (تحليل imports)

مذكور في tsconfig.json:compilerOptions.paths, tailwind.config.js:content, package.json:scripts

[ARCHIVE] أرشيف / [BACKUP] احتياط / [OLD] قديم — مواقع النسخ والحفظ مثل _ops/backups/, _ops/exports/, ARCHIVE_OLD

[DUP] مكرّر — نسخة مطابقة/قريبة (اسم + حجم/Hash) لملف حي في موقع آخر

[BIN] مخرجات بناء — .next, dist, coverage, playwright/.cache…

[VENDOR] طرف ثالث — node_modules وما شابه (لا تُفصّل محتواها بالكامل—لخّص)

[ASSET] أصول — صور/خطوط/ملفات عامّة (مثل public/hero.webp.webp)

[CONFIG] تهيئة — package.json, next.config.*, postcss.config.*, tailwind.config.*, tsconfig.json, .env*

[LEGAL] قانوني — صفحات الشروط، الخصوصية، DMCA، 2257

[TEST] اختبارات — Playwright/e2e/fixtures

[LOG] سجلات — *.log, _ops/logs/**, reports/**

مهم: أضِف حقل Why/Reason لكل عنصر يشرح لماذا موجود، كيف أُنشئ (مثال: snapshot صنعه وكيل/سكربت بتاريخ X)، وما علاقته بوظائف DitonaChat (واجهة/دردشة/مدفوعات/مراقبة/…).

🧱 هيكل ملف الـMarkdown المطلوب
1) Overview / نظرة عامة

الحجم الكلّي للمشروع (تقريبي)، عدد الملفات والمجلدات.

جدول الجذور المرشّحة (أي مجلد يحوي package.json أو src/app أو .next):

Root Path	Kind (canonical_snapshot/working_copy/restored/archive)	package.json	has src/app	has .next	Files/Size	Status Tag	Last Modified	Why/Reason
2) Tree (Full) / الشجرة الكاملة (مع السبب)

اطبع شجرة تفصيلية من الأعلى للأسفل (استخدم أكواد tree و/أو Mermaid).

لكل عنصر:

<path>
├─ src [LIVE] — Why: مصدر التطبيق؛ مستورَد بكثافة؛ ضمن tsconfig paths
│  ├─ app [LIVE] — Why: صفحات Next/Layouts؛ مشار إليها من router
│  │  ├─ chat [LIVE] — Why: صفحة المحادثة؛ تحتوي ChatClient.tsx
│  │  │  └─ ChatClient.tsx [LIVE] — imported-by: src/app/chat/page.tsx
│  │  └─ ... 
│  └─ components [LIVE] — Why: مكونات UI حيّة
├─ _ops/exports/canonical_snapshot_20250104-011400 [ARCHIVE] — Why: لقطة مبرمجة للسلامة
├─ ARCHIVE_OLD [ARCHIVE] — Why: أرشيف قديم قبل التنظيف
├─ .next [BIN] — Why: مخرجات بناء Next.js
└─ node_modules [VENDOR] — Why: تبعيات npm (ملخّص فقط)

3) Components & Pages Map / خريطة الصفحات والمكوّنات الحرجة

صفحات Next (src/app/**/page.tsx, layout.tsx): المسار + الحالة + Why

مكوّنات الدردشة (تحقّق وجودها وتحديد مواقعها):
@/components/chat/{PeerHeader, LowerRightQuick, FriendsList, ScreenEffects, TranslationToggle, ChatComposer, ChatMessages, PeerMeta, Toolbar, ARMasks}
ومشتركات: @/components/{UpsellModal, HeaderLite}
وHooks: @/hooks/useVip

Item	Found At	Status Tag	imported-by	Why/Relation
4) Imports & Build Signals / إشارات الاعتمادية

من كل جذر [LIVE]:

عدد الملفات التي تستورد من @/*

tsconfig.json: paths (أعرضها)

tailwind.config.js: content (أعرضه)

package.json: scripts

المراجع المكسورة (imports لا تملك ملفات): قائمة بالمسارات المتأثرة.

5) Duplicates / المكرّرات

جدول الملفات المتطابقة/القريبة (اسم + حجم و/أو Hash):
| FileName | Path A | Path B | Verdict | Why two copies exist |

6) Large Files / أكبر 30 ملفًا

خارج node_modules و.next: الاسم+المسار+الحجم+الوسم+Why.

7) Assets & Env & Legal

Assets (مثال: public/hero.webp.webp): الموقع، الحجم، من يستعمله.

وجود .env (لا تطبع القيم)، فقط المفاتيح المعروفة المُستخدمة بواسطة الكود (إن أمكن الاستدلال).

Legal pages: المسارات والحالة.

8) Findings & Notes / الاستنتاجات

أين الكود الحي؟ ما الجذر المُرشّح ليكون Canonical Root ولماذا؟

ما الذي ليس ضمن مسار البناء/النشر؟

قائمة واضحة بما يلزم دمجه لاحقًا (إن وُجد)، بدون تنفيذ—توثيق فقط.

📏 متطلبات شكل الإخراج

استخدم جداول واضحة + شجرات؛ أضف أقسام Why/Reason لكل عنصر.

التواريخ بصيغة UTC.

لا تُضمّن محتوى الملفات؛ فقط المسارات والأحجام والمؤشرات (imports/paths/content) وسبب التصنيف.

⛔️ ممنوعات

لا تعديل/حذف/نقل ملفات.

لا npm install، لا build، لا format.

لا تحديث Git. قراءة فقط.

✅ معايير القبول (املأ هذا البلوك في نهاية التقرير)
-- Acceptance --
START_DIR=<absolute path to workspace>
ROOTS_FOUND=<N>
ROOTS_TABLE=present (yes/no)
FULL_TREE=present (yes/no)
WHY_COLUMN=present (yes/no)
COMPONENTS_MAP=present (yes/no)
BROKEN_IMPORTS=<count>
DUPLICATES=<count>
TOP_LARGE_FILES=<count>
ASSETS_SECTION=present (yes/no)
ENV_KEYS_SUMMARY=present (yes/no)
LEGAL_PAGES=present (yes/no)
NODE_MODULES_SUMMARY=present (yes/no)
NO_CHANGES_DONE=attested
REPORT_PATH=_ops/reports/FULL_TREE_AUDIT_<UTC-YYYYMMDD-HHMMSS>.md
-- End Acceptance --


شكرًا—أريد هذا التقرير ليكون مرجعًا نهائيًا يُظهر كل ملف ومجلد، ولماذا يوجد، وما علاقته بالمشروع، بدون أي تغيير على المشروع. وفي نهاية الملف قدم وصفا بصريا للمشروع الفعال من الجزر الفعال كيف يبدو شكل الصفحات وتقسيماتها ومحتوى كل صفحة ووصف تقني , وبالنهايه انشأ ملف مضغوط zip للمشروع الفعال من الجزر الفعال